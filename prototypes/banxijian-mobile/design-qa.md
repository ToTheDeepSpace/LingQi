# 伴戏间交互草稿验收

日期：2026-09-13 更新。范围：本地原型，不是生产／真机／资金验收。9月11日部分保留为历史。

final result: blocked

## 2026-09-13 本轮复测

- 本轮 source visual truth：同一预览在修改前的委托页原生截图（任务内 `marketReference`），以及用户已经确认的“百科与委托并重、红黑榜独立”方向。首页、百科与事件内容扩展是授权的产品变更，不做不同内容的逐像素相似度打分。
- implementation screenshot：当前任务原生截图，URL http://127.0.0.1:5182/；没有可导出的文件路径，不虚构证据文件。
- 1400×1200 浏览器，手机内容 DOM 为393×852，坐标503.5／174；再次调用内容 clip 返回393×852空白。原生整页显示正常。严格1:1内容裁剪与局部归一化对照仍未通过，不能宣布最终视觉验收完成。
- 同一工具输出同时查看修改前后委托页：主标题、筛选、双人像卡片、蓝金配色和价格未改；新增首页后导航从四项改为五项是预期改动。旧源图稍模糊，不能据此判断字体渲染差异。
- 字体：保留系统中文无衬线，标题21px、百科姓名14px、说明11px；元信息10px仍是P3可读性优化，不冒充生产无障碍验收。
- 间距：首轮首页的委托卡片落在底栏后；将首页事件缩为摘要、委托卡片并列，首页分节间距改14px／8px。后续1280×720整页截图中，两份百科、事件摘要及两张委托卡片均在首屏导航上方，未出现水平溢出，device-screen.scrollTop=0。
- 颜色：暖白背景、蓝色操作、金色价格不变；红／黑／白榜通过文字标签及不同浅色表达，不仅依靠颜色。
- 资产：全部复用原品牌及AI人物、现有Radix图标，无新增自绘图像。启动时服务已停止造成新图片加载失败，恢复本地服务后重载，页面图片naturalWidth均大于0。未改28个受保护运行时文件。
- 文案：收录、本人认领、任职确认、委托开通相互独立；黑榜使用“演示店家·乙 · 虚构对象”，没有真实指控；回应状态不等于责任裁决。
- 交互通过：全部／剧本分类、关键词无结果与清除、剧本→关联DM→返回原剧本、红黑榜独立入口／黑榜筛选／事件详情、同意切换反对且互斥、欢乐独立、评论进入本地待审核、投稿进入本地待审核。1280×720原生输入及清除条件后收起键盘已截图复测。保留首页预览，恢复浏览器默认尺寸。
- 控制台：只见启动前01:31的两条Vite断连记录；恢复服务后的上述交互没有新增warn/error。未清除历史日志掩盖问题。
- 构建与检查：原型类型编译、构建、28项运行时文件完整性、4项静态打包测试、4项百科数据测试通过；新增应用文件定向ESLint通过。根仓库全量ESLint会扫到原型受保护模板并报14个既有规则错误；未修改模板绕过。独立生产版本1048d7e的全量Lint、274项测试及前后端构建通过；开发分支业务测试286项通过。
- 当前剩余门槛：截图工具提供有效内容区域捕获后，补齐严格局部对照。真实API、数据、锁档、资金与微信提审均不属于本次原型完成状态。

阻塞项仅为严格的一比一内容截图证据：内置浏览器原生整页截图可以正常显示，`tab.screenshot({clip})`却返回空白内容区域。不能把包含手机外框的整页对照冒充已完成的内容裁剪验收。当前先交互草稿供用户看方向，不宣布最终设计验收完成。

## 来源、状态与尺寸

- source visual truth path: https://jumulu.jusichen.com/commissions ，本轮在内置浏览器采集。它是现有产品的颜色／控件风格参考，不是新市场的逐像素目标稿。
- implementation screenshot path: 当前任务内置浏览器原生截图（未得到可保存的本地文件路径）；实现URL http://127.0.0.1:5182/ 。不虚构截图文件。
- 来源：393×852 CSS视口的委托需求列表，未登录、暖白底、蓝色主按钮、金色选中态、无进行中记录。
- 实现：iPhone屏幕393×852 CSS，外部测试视口1400×1200，DOM核实缩放为1、devicePixelRatio为1。原生完整截图1400×1200，手机区域在x503.5／y174，宽393、高852；裁剪API返回393×852空白，不可作为有效证据。
- Pixel 10屏幕427×952 CSS；另在1280×720窗口进行iPhone原生点击／输入检查。未独立复测1366×768，也未验证真实微信客户端。
- 来源文件的原始PNG像素头信息未导出，不能声称完成原始像素密度归一化。
- 状态差异：来源是需求空状态，新稿是自主上架的委托师目录；这是确认过的产品方向改变。无克隆精确度评分，不将不同内容判为设计漂移。

## 对照与发现历史

1. 初次完整截图发现底部导航安全区透出正文：修正应用自有底栏背景、padding和安全区延伸；不修改模板运行时。后续完整截图已无透出。
2. 图标资产初选命中uni-app默认绿色U：替换为现有剧幕录正式mark。后续对照显示蓝金mark与原导航图标。
3. 窄窗口输入框落在固定提交栏后：新增应用层焦点避让，仅滚动当前MobileScroll。原生点击复测：输入框底部351.80，提交栏顶部360，外层device-screen.scrollTop为0；截图可读输入内容及提交按钮。
4. 缩放状态下自动定位点击曾误滚外层手机框，不能据此认定用户原生点击有同样问题；重载后用原生AX点击复测，外框保持scrollTop=0。后续窄窗口测试应继续使用原生点击，避免混用坐标体系。
5. 源页截图和实现完整原生截图已在同一工具输出共同查看；可确认品牌、图标、人像裁切、按钮及布局的粗对照。因内容裁剪输出空白，尚不足以关闭严格的全视图／局部区域归一化比较门槛。

## 五项检查

- 字体：中文系统无衬线作为手机界面，标题21px、人物17px、正文13px、操作13px；原页少量宋体标题是旧页差异，未声称逐像素复刻。小型演示标签9—10px属后续可加大的P3改善。
- 间距／布局：16px内容边距、12px双栏间距、小圆角轻边框；预约表单首屏和固定CTA可见。键盘避让修正已原生操作复测。深内容通过MobileScroll查看。
- 颜色：沿用暖白、蓝色CTA、金色价格与选中态；错误使用红色、成功使用绿色。未做完整WCAG对比度／文字放大审计，不据此宣布生产无障碍通过。
- 资产：使用正式品牌mark及原有图标，两个清晰AI虚构人物匹配演绎场景，卡片裁切保留面部，无自绘占位图。照片和虚构评分始终标为示例。
- 文案：入口不是人工派单；车马费单列，20%不是平台费；付款、余额和审核均明确演示；保证金不承诺质量认证或即时到账。

## 主交互验证

- 预算800以内只留下知月；恢复不限后两人均显示；暂停林川后不在可预约列表。
- 林川详情 → 免费咨询 → 输入并发送本地消息 → 预约；聊天无外发。
- 缺日期、缺地点均阻止提交；9月19日＋示例地点可创建申请。
- 订单服务1000／交通300／预约500／尾款800；卖家确认、预约付款、见面尾款、完成、售后状态均可演示切换。
- 保证金500→1000模拟升档；服务未完成／售后未结束时退还按钮禁用。
- 委托条改为1200后进入模拟审核，公开报价和旧订单仍为1000。
- 安卓需求发布表单可输入、提交，返回需求列表显示“演示待审核”；安卓键盘与底部导航区域可见。
- 浏览器检查未记录warn/error。类型检查、构建、28个运行时文件完整性检查通过；4项Sites静态包测试通过（不代表已发布）。

## Open Questions / 剩余边界

- 内容区域裁剪证据需修复后再做严格视觉复验；当前final result保留blocked。
- 用户已确认保留委托设计并补强百科／红黑榜；本轮改版已落实到原型，不能等同于真实小程序已发布。
- 真锁档、取消补偿、售后裁决、支付及退款不是本交互稿可验证的内容。

## Implementation Checklist

- [x] 核心路径可点击，不接外部业务写入。
- [x] 品牌素材、安全区、表单焦点问题修正。
- [x] 本地构建和运行时完整性验证。
- [ ] 补齐一比一手机内容截图／局部对照，重跑视觉验收。
- [ ] 用户确认方向后分批接入真实小程序与API。
