# 剧幕录 DESIGN.md

> 本文件是剧幕录仓库内的设计入口，避免 Agent 只读仓库时拿到旧规则。
> 完整设计系统主文档在：
> `/Users/mima0000/Documents/Obsidian Vault/01_Projects 项目/DESIGN.md`

更新时间：2026-07-12

## 品牌定义

剧幕录当前对外定位：致力于成为沉浸式娱乐领域最可信的卡司评分榜。

品牌口号：幕前有演绎，幕后有记录。

正式主域名：`jumulu.jusichen.com`。前台品牌只展示中文“剧幕录”，不再并列旧中文名或英文转写。正式 Logo 资产位于 `public/brand/`。

首屏必须直接说明剧幕录提供什么服务，不再解释“剧幕录”两个字的字面设定。核心用户是剧本杀和沉浸式娱乐参与者；评分对象从 DM 开始，逐步覆盖 NPC、场控和其他卡司。

对外核心表达：

- “幕前有演绎，幕后有记录。”
- “致力于成为沉浸式娱乐领域最可信的卡司评分榜”
- “从 DM 开始，记录真实口碑。”
- “查各地各店卡司口碑”
- “红黑榜是评分榜的事件媒介”

禁止：

- 不要在当前产品文案里使用低质陪伴类旧俗称。
- 不要在当前产品文案里使用临时加戏类旧俗称。
- 不要把委托人写成 coser。
- 不要把“预约意向金”写成“加微信门槛费”。
- 需要表达沉浸式娱乐里的工作人员时，优先使用“卡司”“DM”“NPC”“场控”“演绎协作”等词。

## 当前视觉方向

- 用户侧页面使用暖白纸面 `#FFFDF8`、白色卡片、深色正文 `#1F2937`。
- 辅助文字不要低于 `rgba(71,85,105,0.76)` 这个可读性级别。
- 金色 `#D9A857` 用于签订、认证、星标、契约币、付费入口。
- 浅蓝 `#275389` 用于链接、返回入口和信息提示。
- 紫色只作为灵性光泽或特殊徽章，不作为大面积主背景。
- 红色只用于风险、驳回和黑榜提示，不做大面积背景。

## 页面规则

### 全平台一屏优先

- 所有主要入口、登录注册、发布入口、创建入口、设置页、新手引导和高频后台任务，都默认按“全平台一屏优先”设计。
- 这里的全平台包括手机、平板、低高度笔记本和常见桌面视口；不是只优化手机端。
- 如果当前视口本来能一页展示完，就不要因为 logo、长说明、footer、低频入口或装饰元素制造滚动/翻页。
- 说明文字默认收起、后置或做成小提示，核心表单、主按钮和下一步动作优先留在第一屏。

### 一次性告知降级

- 看过一次就够的解释、字段帮助、低频规则背景、未来规划说明，不要长期占据页面主视觉；默认收进 `?` / 信息图标、折叠区或可关闭教程里。
- `?` 提示要足够小，不改变主按钮、表单和核心数据的布局；点击后要能点外部关闭、自动消失，并且手机端不能溢出屏幕。
- 动态状态不能折叠：错误、成功、审核中、驳回、待处理、支付结果、草稿恢复、空状态和加载状态必须直接显示。
- 发布责任、违法风险、证据/隐私打码、协议同意等会影响合规和用户责任的内容，提交前仍要显性展示，不能只藏在问号里。
- 法律页、审核规则页、隐私政策、用户协议本身就是阅读页，不套用“全部折叠”的要求。

### 主要列表页统一外壳

- `/dm`、`/commissions`、`/carpools`、`/rankings`、`/scripts`、`/guides` 使用同一套剧幕录内容页骨架，不能各自另做一套大标题和背景。
- 全站只保留一条主导航。首页左侧显示品牌；除首页外统一显示返回上一级。返回按钮必须贴近视口最左侧，不能受居中内容宽度限制，也不能再用品牌/回首页按钮代替。
- 内容区不得再重复“剧幕录 / 当前模块”或仿造第二条导航栏。模块切换与页面动作使用无边框的紧凑工具行，手机端动作可换到第二行，但不能挤压主导航。
- 角色点评属于红黑榜口碑模块的内部入口，不在全站主导航和口碑工具行各放一次；全站主导航只保留“红黑榜”，角色点评与城市口碑从该模块的工具行进入。
- 工具行下只保留一块紧凑白色介绍区，再依次展示筛选和真实内容；删除低信息密度的大面积渐变 Hero、装饰型榜单和长期占位的未来功能说明。
- 筛选区使用白底、细边框、8px 圆角；内容卡片保持扁平、易扫描，不嵌套装饰卡片。红黑榜与角色点评可以保留口碑模块专属子导航，其他业务页只共享外壳，不复制口碑投票控件。
- 发布责任、证据和隐私规则必须在发布页面提交前显性展示；列表页不重复铺一整块责任说明，避免挤走首屏内容。
- 内容页共享页头必须保持紧凑，不再承担大 Hero。桌面高度以约 `70–74px` 为基线，标题、说明在左，全部功能按钮在右侧同一行且不得换行；手机端标题区与按钮区可分行，但按钮本身仍保持单行横向浏览。
- `/dm` 只展示 DM，不再显示“全部档案 / DM档案 / 店家档案”类型切换，也不额外显示“当前为 DM”的固定标签。店家档案与店家评分继续使用独立 `/stores` 页面。
- 城市筛选必须复用完整固定城市库，不能从当前返回数据反推选项，也不能只列热门城市。微信小程序使用可搜索的底部抽屉，支持按城市名及所属省份 / 区域匹配、点击立即选中，并持久化用户上次选择；DM、店家、红黑榜和拼车必须使用同一组件与同一城市字典。
- 微信小程序所有页面统一使用一条自定义导航栏：页面名居中，除首页外左侧显示真实返回；直接打开子页且无页面栈时，按业务关系回到上级页或首页。内容区不得重复同名大标题。
- 微信小程序内容页按“导航栏、紧凑上下文、单一工具区、真实内容”排列；筛选、搜索和主要动作合并进同一工具区，表单取消无意义外层卡片并固定主要提交动作。
- 红黑榜发布入口同时放在榜单页和 DM / 店家详情页；从详情页进入时预填对象及城市，首屏必须看得到核心表单与提交路径。

### 首页

- 第一屏不解释品牌字面含义，直接说明平台提供 DM 评分、卡司口碑与事件记录服务。
- 第一屏必须完整展示当前主要入口，不能让用户为了看全主入口按钮再下滑一点；导航栏高度要算进首屏。
- 服务者、委托人、配套服务的关系要清楚。
- 配套服务包括摄影师、妆造师、服装商、道具师。
- 不再把 coser 作为主要服务对象写进首页。

### 服务大厅

- 要像“可被委托的角色入口”，不是资料表。
- 只展示已经开启服务、提交服务并通过审核的用户；普通注册用户、只逛榜/只投票/只评论的玩家，不进入服务大厅。
- 重点展示头像/封面、角色气质、可接地点、档期、社交账号快照、精选评价和委托入口。
- 支持抖音、小红书等外部主页链接；手机端优先尝试打开 App，失败再打开网页。

### DM / 店家档案认领

- DM / 店家档案列表在手机端采用紧凑横向摘要：左侧 `72px` 小方图，右侧放名称、认领状态、城市/店家和最多一行简介；评分统计改为一行文字指标，标签最多显示 2 个。评分、写评价、外部社交平台和剧幕录站内主页入口必须保持在同一行；社交平台用可识别的小图标，不用“个人主页”长按钮。
- 用户可以为头像和档案图选择缩略图焦点；支持浏览器人脸检测时自动定位，不支持时默认偏上居中并允许点击/拖动调整。原图不得被破坏性裁切。抖音、小红书等输入框必须接受整段分享文案并自动提取其中 URL，前后端都要容错，不能要求用户手工删掉分享文字。
- “本人认领 / 店家认领”是低频次要动作，放在“未认领”状态旁边，用紧凑文字按钮表达；不能和“查看评分 / 给TA评分”并列成同尺寸主按钮。
- 社区新建或补充未认领档案免费；本人认领 DM / 店家档案在提交身份材料前支付固定平台核验服务费 `8.88 元`。付款不是认证通过承诺，仍须提交证明并经人工审核；审核驳回后，同一账号对同一档案可沿用已付资格补充材料，不重复收费。
- 所有付费服务必须先完成手机号验证。微信首次登录形成临时账号时，若手机号已属于网站账号，必须先合并回原账号再创建订单；购买资格只记在合并后的主账号上，临时账号不得重复领取新户权益或独立持有付费资格。
- 认领必须登录后提交完整审核表单，至少包含证明类型、关系说明、1–3 张身份凭证截图和真实性确认；不能退化成浏览器 `prompt`。
- DM 可提交社交账号后台、任职/排班证明或其他材料；店家可提交营业执照/主体资料、店铺平台后台或其他材料。页面要提示用户先遮住证件号、手机号、聊天对象等无关敏感信息。
- 身份凭证只供管理员核验：必须存放在公开静态目录之外，公开档案接口不返回文件路径，只允许管理员鉴权接口按文件读取。
- 每次认领申请独立留痕，保留原始关系说明、证明类型、文件元数据、审核结果和驳回原因；驳回原因不能覆盖用户原说明。
- 管理后台的“档案审核”统一承接新建档案和认领申请，并直接展示私密凭证缩略图。通过认领后档案绑定申请账号；驳回后允许补充材料重新提交。
- 微信小程序的 DM / 店家列表必须同时提供“评价”和“新建档案”入口；搜索不到对象时可用当前关键词预填新档案。评价表单内的 DM / 店家选择器必须可搜索，并允许创建待审档案后与本次评价一起提交，不能强制用户退出评价流程另行建档。社区新建档案无需主动提交证据，统一标注“社区提供”并沿用网站后台审核规则；待审档案不能混入公开列表。
- 管理后台不得直接向管理员展示 `profile_id`、`claimant_id` 等内部 UUID。认领申请账号必须解析为手机号、邮箱或微信账号，并同时显示昵称；资料暂时未加载时显示可理解的状态提示，不回退展示内部代码。
- DM / 店家卡片本身是进入专属页的主入口，整卡可点击；不要在卡片上增加大型“编辑 / 补充资料”按钮。编辑入口只放在详情页顶部状态区，用紧凑文字按钮表达：认领人显示“编辑我的档案”，其他用户显示“补充 / 纠错资料”。
- 档案修改同时按认领状态和字段类型分流。认领人本人编辑时，身高、体重、MBTI、星座直接生效，城市先生效后审核，自由输入内容管理员通过后公开。他人编辑已认领档案时，先观察 3 天：认领人期间上线则必须本人同意或反对；期间未上线则按字段规则继续处理，不能让自由输入内容绕过管理员前审。未认领档案的社区补充全部交管理员前审。资料修改不得顺带改变认领关系、评分或事件，DM 改店仍须对应店家确认。
- `/dm` 默认使用紧凑卡片列表，不显示“综合 / 体验 / 玩家”三块空统计。存在评分时只显示一行评分摘要；卡片整卡进入详情，只保留紧凑的“+评价”次要操作。
- DM 列表支持按姓名、城市、店家、档案标签、审核通过的评价标签、常开剧本和评分状态筛选。关系图是列表旁的辅助视图，不取代默认列表；节点可包含 DM、店家、标签、剧本和被圈档案，图组件必须按需加载。
- DM 详情支持最多 9 张照片。第一张为封面；每张照片独立保存排序、说明和显示焦点，编辑人可调整封面与顺序，访客点击后查看完整大图，原图不得被破坏性裁切。
- DM 百科可补充入行时间、出生年份、身高、体重、MBTI、星座、简介、常开剧本、从业经历、圈人和圈店。MBTI 与星座按普通公开资料处理，不视为隐私字段；Tag 功能继续保留，圈人圈店使用结构化档案关系，不只保存无法跳转的纯文本。
- 出生年份、身高、体重属于敏感百科字段，仅本人编辑或本人明确同意后才能公开。他人补充未认领档案时不得提交这些字段；管理员审核混合修改时，没有本人同意的敏感字段不得随其他资料一起通过。身高只接受 `100–250 cm` 整数，体重只接受 `30–300 kg` 整数；星座限定 12 项、MBTI 限定 16 项，城市必须从固定城市库选择。字段范围和枚举必须同时由前端、API 与数据库约束。
- DM 评价选择店家时必须同时保存剧司辰店家 ID 和剧幕录店家档案 ID。已有档案的店家（例如 O·G沉浸式剧场）不能退化为纯文本，评价详情中的店家应可进入专属主页。

### 个人主页后台

- 手机号、邮箱和小程序首次注册都必须填写昵称。昵称是公开展示名，可直接用于个人主页和用户发布内容；登录账号仍是手机号、邮箱或微信身份，昵称后续可由平台治理但不做发布前审核。
- 主帖、评论、委托、拼车、攻略、评分和相关方回应等页面只要公开显示发布人，就应使用真实 `profile_id` 链接到该用户公开主页；不能按重名昵称反查。投票与缠头支持匿名，匿名时不暴露用户主页。
- 小程序所有业务页右上角统一保留紧凑“消息”入口，未读状态只用红点提示，不新增大号通知卡或第二套消息中心。入口进入既有消息通知页；消息页本身隐藏重复入口。委托申请、处理结果、账号状态和管理员回复都汇入同一列表。

- 个人主页后台属于工作台，直接复用全站 `Navbar`，不得再自建第二套品牌、公开入口、邀请、认证、退出和管理顶栏。
- 后台语义用内容区紧凑的“个人后台 / 当前模块”工具行表达，只保留“预览公开主页”这一项页面动作；模块导航放在左侧 sidebar，不在顶栏堆 breadcrumb。
- PC 左侧导航保持紧凑并固定在全站导航下方；手机端改为全站导航下方的横向模块切换，8px 圆角、无浮夸阴影，不占据大块首屏。
- 公开个人主页和后台使用同一套暖白底、白色面板、细边框、8px 圆角。公开主页不再使用渐变 Hero 或页面内重复返回；头像、昵称、城市、身份和认证进入紧凑首屏身份区。
- 普通玩家公开页显示“个人档案”，只有确实发布服务或可接角色的用户才显示“服务者档案”和委托入口。
- 钱包余额、认证身份、邀请奖励必须收进 `/dashboard/*` 子页，和公开资料、服务与作品、账号安全、我的发布使用同一套后台外壳；旧 `/wallet`、`/certification`、`/referrals` 只作为兼容和完整流程入口。
- 服务与作品页的主线是“是否提供服务 -> 提供什么服务 -> 已添加服务 / 新增服务”。作品集和可约档期拆到 `/dashboard/services/works` 与 `/dashboard/services/availability`。
- 选择“暂不提供服务”后，服务页只保留开关，不继续展示服务表单、城市、角色、档期或作品内容。
- 选择“提供服务”后必须提示用户：服务审核通过后会出现在服务大厅；未审核通过前不会公开展示。
- 服务主线下方不放常驻城市、流动状态、微信或预约意向金；常驻/流动状态、可接城市、微信属于公开资料语义。预约意向金后续若保留，应放到具体服务配置内，而不是全局服务页首屏。
- “可接本与角色”只对角色委托师、自由 DM 等角色型服务展示；摄影师、妆造师、服装商、道具师等非角色型服务不需要填写。
- 服务上线审核按“整块服务清单”提交，不把价格、时长、说明拆成独立审核项；价格这类纯数字标定只允许输入数字并按字段校验，不进入内容审核语义。
- 可约档期用日历多选交互：点击日期只切换本地选中态，用户确认后一次性提交审核；同一天已公开或正在审核时不能重复提交，前台必须有清晰视觉区分“已公开 / 本次选中 / 忙碌”。

### 委托需求墙

- 委托必须作为独立页面和独立入口存在，不能并入拼车页；小程序底部导航保留“拼车”，委托从首页独立进入。
- 委托人可以发布任务需求：执行城市、日期、想要的角色、预算、补充说明；执行城市为必填，其余结构化细节可以后补。
- 需求也可以只是一段自由文字，不强迫所有人填完整表格。
- 需求墙应该像“愿望委托板”，不是招聘网站。
- 任务地点和日期必须突出展示，因为服务者可能全国流动。
- 发布人可设置“接受异地委托师远征”。关闭时仅常驻执行城市的委托师可申请；开启后，主页“可服务城市”包含执行城市的异地委托师也可申请。
- 浏览委托默认恢复用户上次选择的城市；没有历史选择时使用首个关注城市。城市内同时展示本地常驻委托师与声明可远征到该城市的委托师，并明确标注两类匹配关系。
- 预留 AI 填表助手接口。
- 当前 `0.1.9` 只收取平台信息展示与联系方式解锁服务费，不代收真实委托价款，不做担保、分账或抽佣；不能拿小程序虚拟支付替代真实委托结算。正式交易收款继续预留，待商户和个人卖家结算资料完整后再接。
- 委托师可维护一张公开“委托条”：一张主图、一句话介绍、补充说明、整数身高 `100–250 cm`、整数体重 `30–300 kg` 和擅长角色类型。首次提交委托条固定支付 `8.88 元`，审核通过后上架；后续修改不重复收费但仍需人工审核，审核期间继续展示上一版已通过内容。
- 委托师必须另填一项经过审核的业务联系方式，不得自动复制登录手机号、登录邮箱或微信 OpenID。委托师可暂停联系方式展示；暂停不删除用户已经购买的永久解锁资格，恢复后无需再次付款。
- 用户从委托师列表或个人主页支付固定 `8.88 元`后，服务器确认微信支付成功即解锁该委托师当前已审核的业务联系方式。同一账号对同一委托师只支付一次并永久保留解锁资格；委托师更新已审核联系方式后，已解锁用户看到新联系方式。
- 付费订单只能由微信小程序登录且已完成手机号验证的主账号创建；服务类型、目标对象和 `8.88 元`金额均由服务器决定，客户端不得自定义。资格仅在服务器核验微信支付成功通知或主动查询确认后授予。
- 网站与小程序共用同一付费权限，网站不得绕过收费直接返回联系方式。首版付款入口只在微信小程序提供，网站未解锁用户应明确提示前往小程序完成支付。

### 红黑榜

- 手机端“全部城市”面板必须固定在可视区域内并允许内部滚动，不能被父容器裁切。城市选择按账号保存在浏览器中；再次进入时优先恢复上次选择，其次才使用玩家主要城市，不能在资料加载后覆盖用户已选城市。

- 分类：店家、DM（卡司）、服务者（委托师）、玩家。
- 城市维度的口碑聚合不要叫“同城口碑”或“城市口碑榜”。泛称可叫“城市口碑”，但用户可见标题/入口优先用“XX口碑”，例如“保定口碑 / 北京口碑 / 上海口碑”。
- 红榜/黑榜首次提交证据选填，发布前必须明确提示审核规则；审核员认为事实支撑不足时可以打回补证。
- 红黑白榜页面要显眼展示发布责任提示：互联网不是法外之地，发布者对事实、证据、隐私打码和言论后果负责。
- 白榜承接非夸非踩的趣闻、笑话和中性记录，原型期免费发布。
- 红黑白事件帖不再提供付费打榜或付费踩榜；口碑票是“同意 / 反对 / 欢乐”，同一账号只保留一张票且可以改票。
- 历史付费打榜、踩榜数值只在事件详情中作为旧规则记录保留，不影响事件流、城市口碑、对象档案或默认推荐顺序。
- 对 DM 的真金支持统一走档案页“缠头”；缠头榜独立存在，不给口碑票、五星评分、审核或事件排序加权。
- 离谱反应免费，表示“这事儿离谱 / 值得围观”，不代表认同或反对。
- 普通评论免费，但仍进审核队列；投票时可以顺带写一条免费评论。
- 黑榜公开展示 30 天后进入已过期记录。
- “我是相关方”不是删帖入口。正确路径是先发免费评论，再提交相关资料申请相关方认证；审核通过后评论置顶在主帖下方。
- 红黑白榜主帖、评论和相关方置顶回应都必须有举报入口；举报要求登录，进入管理员待处理队列。单次或多次举报都不自动删除、隐藏或折叠内容，是否下架由管理员结合证据人工决定。
- 社区观察员可以对管理员列入重点复核的内容提交众审建议，但不能直接下架、恢复、封号、改判，也不能查看隐私证据。

### 拼车区

- 拼车区属于剧幕录口碑与同城协作生态，不是独立产品。
- 发布拼车页优先是“车头消息工作台”：先粘贴/生成车头姐拼车消息，再解析成结构化字段。
- 发布拼车必须包含日期、城市、报名截止日期、车头微信/联系方式、剧本名和拼车说明。
- 解析日期时，过期日期必须提示“已过期/可能不需要拼车”，不能自动推到下一年。
- 拼车区里的“补贴”不是剧幕录契约币，而是车头/恋陪位给其他玩家位的现金补贴或票价折扣。
- 半价、免票、几折、A补、补/减多少必须分开记录，原始补贴短语写进补贴说明，不能压缩成一个“出补”。
- 可加钱提升展示排序，但不得影响审核结论。
- 加权展示消耗契约币，是平台排序功能；它和拼车现金补贴必须在文案、字段和审核口径上分开。
- 公共拼车列表不直接展示车头微信；用户注册登录后点击“联系车头”才显示联系方式。
- 拼车是强时效信息，默认先公开、后治理；低风险拼车不等人工预审。
- 拼车必须有举报入口。举报要求登录，进入管理员待处理队列；管理员可标记处理、暂不处理或下架对应拼车。
- 拼车发布和列表必须提示：互联网不是法外之地，发布者对内容负责；涉及造谣传谣、侵犯隐私、诈骗等情形时，可能承担民事、行政乃至刑事责任，平台依法留存记录并配合处理。
- 已公开拼车后续可同步为剧司辰默认店家的剧本、房间线索或排期草稿。

## 统一发布责任与举报

- 所有发布页都必须展示发布责任提示，至少覆盖红黑白榜发布、委托需求发布和拼车发布。
- 所有公开 UGC 都要有举报入口，至少覆盖拼车、红黑白榜主帖与图片、评论、委托需求、委托条、公开主页、DM / 店家档案与图片、评分、回复、攻略、服务和作品。自己的内容不显示举报按钮，应使用编辑、撤回或下架入口。
- 公开详情页的举报入口统一使用低干扰的小旗帜图标按钮，并提供无障碍名称；不要用整块说明卡或大号文字按钮挤占正文和主操作空间。举报表单里的“提交举报”仍保留为清晰的文字主按钮。
- 举报必须要求登录；提交后进入 `lc_reports`，后台按待处理、已处理、驳回/暂不处理管理。
- 举报不是自动下架。管理员可以根据举报记录、私密证据和平台规则隐藏对应内容，并保留处理记录；系统风险判断只能调整后台处理优先级，不能直接改变公开状态。
- 图片举报必须记录精确图片标识，避免只能举报整页。举报说明和最多 3 张证据图片均为非公开材料，存放在公开静态目录之外，只允许提交者留痕和管理员鉴权读取。
- 小程序“我的”提供问题反馈入口，覆盖功能故障、档案纠错、无效联系方式、支付退款、举报滥用和其他问题。支付类反馈自动关联平台服务购买记录；管理员回复后生成站内消息。反馈图片与举报证据采用相同私密存储边界。
- 对用户文案要明确：平台会保留必要日志，并在涉及传谣造谣、侵权、诈骗、泄露隐私等情况时依法配合相关机构、权利人或监管单位。

## 用户资料与头像

- 用户可以上传头像和封面图。
- 未上传头像时，展示层可以按昵称生成默认头像，避免公共页出现粗糙占位。
- 发布、评论、投票、相关方认证、委托/拼车申请等公开互动必须完成手机号验证；头像用于展示，可鼓励上传，但不再作为发言门槛。举报和联系平台不受此门槛限制。
- 昵称、头像、封面图、简介默认走自动机审；命中风险进入人工复核。
- 被驳回时回退到上一版已通过资料。
- 实名认证只影响星标和后台可信度，前台仍显示昵称和头像，不暴露真实姓名。
- 禁止头像/昵称冒充官方、冒充他人、使用违法违规、色情擦边、血腥暴力、仇恨攻击、联系方式导流或广告内容。

## 审核系统

- 注册账号不需要审核；红黑榜、评论、委托需求、相关方回应等发布内容需要审核。拼车因时效性强，默认先公开、后置治理。
- 发布前提示不是装饰，是责任边界：所有发布页都要在提交前让用户看到证据、打码、实名/账号留痕和违法风险。
- 自动预审先检查文本、图片、链接、重复内容、证据缺失、第三方隐私、辱骂和导流。
- 机器审核保存 `risk_score`、`risk_labels`、`model_version`、`reason`。
- 机器不能独立完成严重处罚、永久隐藏、封号、公开定性这类不可逆决定。
- 高风险黑榜、相关方争议、删除/隐藏、热门帖改判、涉及隐私/严重指控的内容，走多人复核。
- 众审建议台只开放给管理员、创始推荐人和有效期内社区观察员。众审只看对象摘要和公开内容摘录，不看上传证据、手机号、联系方式、认证材料等隐私信息。
- 众审建议只做参考，不替代管理员最终处理。管理员后台必须能看到同对象有效举报数、自动动作、风险等级和众审建议汇总。
- 建议统一 `moderation_jobs` 队列管理主帖、评论、头像、昵称、委托、拼车和相关方申请。

### 账号限制与申诉

- “账号已合并”是账号归并状态，不是违规处罚。旧临时账号只保留为合并记录，必须清除发布/账号限制范围；旧令牌再次请求时应清理本地登录态并提示使用主账号重新登录，管理员不能把合并记录当作普通封禁解除。
- 真实账号限制必须记录范围、原因、开始时间和可选结束时间。限制范围分为“仅限制发布”和“限制整个账号”；仅限制发布时仍允许查看账号状态、接收站内通知、提交申诉和完成必要的账号绑定操作。
- 限制生效、调整、解除、申诉补充要求和申诉结论都必须生成用户可见的站内通知。受限用户在网页和小程序中都应看到明确入口，不得只收到笼统的“被限制发布”错误。
- 同一账号同时只保留一个处理中申诉；管理员可要求补充、驳回或解除限制。解除时必须明确选择是否恢复此前因处罚隐藏的公开主页，不能把解除登录/发布限制与恢复公开展示混成一个隐式动作。
- 到期限制在鉴权时自动失效并通知用户；管理员也可提前解除或调整范围。所有处罚、申诉和处理动作继续写入安全日志与审核记录。

### 管理后台视觉

- 超管审核页属于工作台，不走暗黑仪表盘风格；默认使用剧幕录暖白纸面、深色正文和金色轻强调。
- PC 端优先左主右辅：左侧是审核队列和处理动作，右侧是数据概览；不要让统计卡片占据主流程上方。
- 移动端折叠为单栏，标签条允许横向滑动，审核动作按钮自动换行。
- 低频说明保持短句，不能用大段说明挤占审核内容；证据、正文、处理按钮要比装饰和统计更显眼。
- 管理后台必须有“全部待审”聚合入口，按时间展示所有未审核对象；细分 tab 只用于深入处理，不要把 tag、评论、主帖、资料改动拆成一排难扫的主入口。
- 用户自建 tag 不应形成独立噪音队列；待审 tag 要标在它所属的具体待审核内容后面，例如某条角色评分、公开档案或主页资料改动。
- 用户账号治理单独成栏，能查看所有近期账号，而不是混在待审内容里。
- 红黑白榜审核允许管理员改判类型后通过；打回重写必须写入用户可见原因，不能只留后台日志。
- 管理后台的自动预审/风险提示必须保证可读对比度，不能用浅橙/浅红文字压在米色底上；`review` 状态用浅橙底配深棕橙正文，`block` 用浅红底配深红正文，`pass` 用浅绿底配深绿正文，摘要文字不得低于 `rgba(71,85,105,0.88)`。

## 防篡改记录

暂不上真正区块链。先做站内防篡改审计链：

1. 通过审核的主帖、评论、相关方回应生成规范化 JSON。
2. 计算 `content_hash`。
3. 保存 `previous_hash`，形成站内 hash chain。
4. 每天生成 `daily_root_hash`。
5. 未来需要时再锚定可信时间戳服务或公链。

对用户展示为“记录编号 / 审核通过时间 / 内容校验码 / 历史状态”即可。
